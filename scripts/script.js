const expandButton = document.querySelector('body section button');
expandButton.addEventListener('click', hidee); 

function hidee() {
  var div1 = document.getElementsByClassName("inhoud").setAttribute(class, hidden); 
}